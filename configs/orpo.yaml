# ORPO hyperparameters for YuhoLens-Pipeline Week 2.
# Reference-free (trl.experimental.orpo.ORPOTrainer).

model_id: output/yuholens-14b-sft
preference_data: data/teacher/orpo.jsonl
output_dir: output/yuholens-14b-orpo
per_device_train_batch_size: 1
gradient_accumulation_steps: 16
num_train_epochs: 1
learning_rate: 5.0e-6
max_length: 8192
beta: 0.1
save_steps: 100
